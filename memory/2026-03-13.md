# 2026-03-13 — Friday

## Ramon Decisions & Mandates

### Email Template Rule (Major)
- **"If it CAN be hardcoded via .py, hardcode it. No exceptions."** — applies to everything, not just emails
- Created `templates/better-hand.txt`, `templates/sunday-ritual.txt`, and `templates/render_email.py`
- LLM only supplies dynamic content (body text, titles, challenges)
- All structural parts (Klaviyo tags, greetings, sign-offs, coupon codes, URLs, P.S. rotation) are hardcoded in templates
- This is now a standing rule for all repeatable processes

### Sunday Ritual Workflow Overhaul
- **No approval gate** — publish immediately, Ramon fixes after if needed
- **No auto-scheduling** — campaign created with send time pre-set but NOT scheduled; Ramon reviews and schedules in Klaviyo manually
- **Subject line**: Campaign name = `The Sunday Ritual #N: [Hook]` (internal). Email subject = `The Sunday Ritual: [Hook]` (no number visible to subscribers)
- **Email structure locked**: body → blog link (anchor tag) → "Warmly, Ramon" → P.S.
- **P.S. rotation**: Reply-engagement questions ONLY. No links, no socials (dormant social accounts, Primary inbox goal)
- **Blog link**: Must be proper anchor tag `<a href="...">Read the full story on our blog →</a>`
- **Dashboard auto-trigger**: Clicking "Select" on the dashboard sends a Slack message to #chloebot that triggers the full pipeline. No polling cron, no wasted cycles.
- Built `sunday_ritual_publisher.py` — handles ALL non-creative work (Shopify, Klaviyo, P.S. rotation, issue tracking, logging)
- **Issue history**: #1 Julia Child, #2 Eva Gabor, #3 Betty White, #4 Lucille Ball (Mar 23)
- **Always work 1 week ahead** to stop last-second scrambling

### Klaviyo API Scheduling
- Klaviyo API DOES support scheduling with `send_strategy.method: "static"`, `send_strategy.options.is_local: true`, and a `datetime`
- Previously incorrectly documented as "UI-only" — corrected in playbooks
- Better Hand → Thursday 9 AM recipient local time
- Sunday Ritual → Sunday 10 AM recipient local time

### PR Opportunity Filtering (Important Lesson)
- **Closed Yahoo Creators opportunity** — query was about skin care products, even though audience (women 50+) matched, the specific query topic is too far from card games
- **Rejected Homes & Gardens opportunities** (Regency-core kitchens, layering rugs) — "Too far away from a potential PR opportunity. Generally people looking for very specific PR opportunities such as these we should not be considered as an opportunity"
- **Takeaway**: Only pursue PR where the query topic has a direct, natural connection to card games/social gathering. Demographic match alone isn't enough.
- **Correct product link**: `https://www.all7s.co/products/canasta-cards-deluxe-game-set` (was wrong before)

### No Socials in Emails
- Remove course link and Instagram from P.S. rotation
- Social accounts are dormant — linking them hurts credibility
- All P.S. options should be reply-engagement questions only

## Infrastructure & Fixes

### Sales & Traffic BQ Schema Fix (9:10 AM)
- Amazon added ~20+ new fields to GET_SALES_AND_TRAFFIC_REPORT (refunds, claims, shipping, aggregate metrics)
- Root cause: two scripts wrote to `sp_sales_traffic` with incompatible flattening approaches
- Fix: Dropped old table (only 132 Airbyte rows), recreated with 70-column schema matching on_demand_report.py format
- Updated `scheduled_report_poller.py` to use new `parse_sales_traffic_json()` function
- Both byDate and byAsin records now stored with `record_type` discriminator column

### Airbyte SP Connection — 3 Consecutive Failures
- Jobs 74658214 (Mar 13), 74509539 (Mar 12), 74361586 (Mar 11) all failed
- All syncs started but completed with very few rows (~400-1000 vs normal volume)
- Suggests partial stream failure or API credential/throttling issue
- **Needs manual investigation in Airbyte Cloud**
- Ads connection: healthy (2.2M rows)

### Daily Sales & Traffic BQ Streaming Buffer Issue
- US marketplace MERGE statements fail when rows are still in BQ streaming buffer (~30 min flush)
- Recommendation: Switch US write strategy from MERGE to INSERT + scheduled dedup query, or add 30-min delay

### OpenClaw Update Check Fix
- Was failing because delivery mode was `"none"` — isolated session couldn't send messages
- Fixed: switched to `announce` mode targeting #chloebot

### Nightly Memory Digest Fix
- Was failing because edit tool couldn't handle large memory files (~10KB)
- Fixed: Changed payload to read existing file first, merge, then write full result

### SP Report Daily Status — False Alarm
- "Consecutive errors" were delivery failures (couldn't post to Slack), not script failures
- The actual data collection ran fine

## Canasta Strategy Dedup (10:05 AM)
- Ramon approved all 22 pending strategy entries
- Found 9 clusters of potential duplicates, 7 true dupes, 2 false positives
- Removed 8 duplicate entries: FB001, FB031, FB032, FB042, CL002, CL003, CL005, CL007
- Result: 148 → 140 approved entries (157 → 149 total)
- Built `canasta-rules/dedup_checker.py` — Jaccard similarity (threshold 0.50)
- **Ramon says**: Don't need approval for future dedup, handle autonomously
- Dashboard approve button bug fixed (duplicate IDs across `id` vs `entry_id` fields)

## PR Activity
- **HARO Morning**: Gift Guide - Budget Savvy Diva (Score 85, ETV 8,159) — sent package
- **SOS Friday**: Yahoo Creators - products for women over 50 (Score 85) — sent package, then CLOSED by Ramon
- **HARO Evening**: Homes & Gardens - 2 pitches sent (Regency-core kitchens Score 90, Layering rugs Score 75) — REJECTED by Ramon
- PR follow-up check: No follow-ups due today. Next outreach follow-ups ~3/17. Next pitch window: Good Housekeeping 3/19.

## Listing Changes Detected
- **Coupons removed**: A7S-CAN6P (CA), GTV-SH2 (CA)
- **Strikethrough gone**: GTV-SH2 (CA) was -15%, A7S-TTFRA (US) was -25%

## AWD Inventory Snapshot
- Canasta Deluxe (B07KBB1WJS): 1,872 units (78 cartons)
- CAN2P (B0BR4FDDV8): 240 units (6 cartons)
- CAN6P (B0G1NRM21J): 96 units (8 cartons)
- GTV-SH2 (B0CVCVGB2K): 432 units (18 cartons)
- NER-RJ (B0B6Q77S57): 24 units (3 cartons)

## Heartbeat Health Checks
- Gmail OAuth: healthy
- Cron lightweight check: no runaway jobs or 3+ consecutive errors detected

## daily_collection.py Updated (11 PM)
- **Orders:** Changed from 60-day to 30-day lookback (Amazon rejects >30 days for this report type — was silently returning empty data)
- **Returns:** Added new Task 3 using `GET_FBA_FULFILLMENT_CUSTOMER_RETURNS_DATA` with 60-day lookback. New `returns_daily` table tracks return date, reason, disposition, customer comments. Links to orders via `order_id`.
- **Inventory:** Added Planning report fallback — if `GET_FBA_MYI_ALL_INVENTORY_DATA` returns FATAL, auto-falls back to `GET_FBA_INVENTORY_PLANNING_DATA` which has richer data anyway (FC transfer/processing breakdown, units shipped 7d/30d).
- Also added `timeout=30` to all `sqlite3.connect()` calls to prevent "database is locked" failures.

## Cron Job Hardcoding Audit (11:30 PM - 12:07 AM)

### Vine Sheet Bug Fix
- Vine order emails were being processed by LLM playbook, causing data corruption:
  - Concatenation bug: all fields crammed into cell A
  - Wrong row targeting (overwrote existing data)
  - Price parsing errors ($159.99 → $0.99)
- Built `vine_order_processor.py`: full pipeline (Gmail API → parse → sheet write → Slack → archive)
  - Uses Gmail API directly (not gog CLI which truncates HTML emails)
  - Extracts ASINs from Amazon's encoded redirect URLs (%2Fdp%2F pattern)
  - Price normalization: Amazon's no-decimal format ($15999 → $159.99)
  - Duplicate detection before writing
  - Brand lookup with fallback to email subject

### Cron Job Audit & Conversion
- Audited all 45 cron jobs. Every single one was `agentTurn` (LLM)
- Categorized: 19 deterministic (just run scripts), ~8 could be hardcoded, ~18 genuinely need LLM
- Built `cron_runner.sh`: generic wrapper that runs command, logs to ClickUp, alerts on failure
- Converted all 19 deterministic jobs to thin LLM wrapper (one-line "Run: bash cron_runner.sh...")
  - Set --light-context on all to minimize token usage
  - Token savings estimate: ~1-1.5M fewer tokens/day
- New scripts created:
  - `cron_runner.sh` — generic cron wrapper
  - `cron_registry_sync.py` — replaces playbook (tested: 48 rows, 321 updates)
  - `openclaw_update_check.py` — version comparison
  - `vine_order_processor.py` — full vine email pipeline
  - `vine_sheet_writer.py` — standalone sheet writer (used by processor)

### Gmail Inbox Router
- Built `gmail_inbox_router.py`: deterministic email classifier
- Handles vine orders, ads reports, FBM sales, external/trusted (archive + log) without LLM
- Only passes HARO/SOS and BCC learning loop to LLM via /tmp/gmail_llm_queue.json
- Updated Gmail Inbox Processor cron to two-stage: script first, LLM only for leftovers

### Ramon's Directive (Important)
"If it CAN be hardcoded, it should. If it can't, then LLMs it is."
- Priority: avoid hallucinations and inconsistencies
- LLM tokens are OK, but deterministic code preferred wherever possible
- This should be a guiding principle going forward
