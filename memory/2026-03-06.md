# 2026-03-06

## PPC Console Reports Import (In Progress)
- Ramon manually downloaded 3 reports from Amazon Ads console and Seller Central
- **Campaign Report (CSV):** 569 rows, 23 cols, Feb 27-Mar 5 2026, US+CA, 83 campaigns
- **Targeting Report (XLSX):** 4,053 rows, 24 cols, Feb 27-Mar 5 2026, US+CA. Uses inline strings (openpyxl reads header only as 1 row — need custom XML parser to extract data). Columns: Date, Portfolio, Currency, Campaign Name, Country, Ad Group Name, Targeting, Match Type, Impressions, Top-of-search IS, Clicks, CTR, CPC, Spend, ACoS, ROAS, Sales, Orders, Units, Conversion Rate, Advertised SKU Units/Sales, Other SKU Units/Sales
- **Orders Report (TXT, tab-delimited):** 1,484 rows, Feb 4-Mar 6 2026, channels: Amazon.com/.ca/.com.mx + Non-Amazon. 1,381 shipped, 53 pending, 50 cancelled
- Ramon confirmed: no "schedule and email" option for orders reports — must be manual download from UI
- Awaiting Ramon's go-ahead to import all three into DB
- Campaign/Targeting → overwrite overlapping Feb 27-Mar 5 in ppc_campaigns/ppc_keywords
- Orders → insert/update by order ID
- FBA Inventory report was previously confirmed flowing fine via SP-API (no manual download needed)

### Technical Note: Targeting XLSX Parsing
- openpyxl fails to read data (shows 1 row) because file uses `inlineStr` cell type instead of shared strings
- Must parse xl/worksheets/sheet1.xml directly, reading `<is><t>` elements for string values
- Date column is Excel serial number (epoch 1899-12-30), needs conversion
- Successfully validated via XML parsing: 4,053 data rows confirmed

## Key Insight from Ramon
- Amazon Ads console reports are more reliable than API for PPC data
- Three report types needed: Campaign, Targeting, Search Terms (all Sponsored Products)
- Reports include both US and Canada via Country column (no need for separate downloads)
- Search Terms report was already received earlier (555 rows, same date range)
- Still need to decide on recurring workflow for these manual reports

---

## Airbyte Cloud Setup (MAJOR — Data Pipeline Overhaul)
- Ramon decided to replace unreliable SP-API/Ads API cron jobs with **Airbyte Cloud** (~$10/month)
- Evaluated Saras Daton ($95/mo — too expensive) and Openbridge before landing on Airbyte
- **Account created under Ramon's company domain** (not chloemercer32@gmail.com) — intentional to avoid bot-flagging risk
- **Amazon Ads → BigQuery** connection configured and first sync started
  - Selected ~15 streams: all SP campaign/keyword/targeting/ad group report streams + entity streams + attribution streams
  - Attribution streams needed Incremental | Append (no primary key)
  - Entity/config streams: Full Refresh | Overwrite Deduped
  - Report streams: Incremental | Append Deduped
  - First sync partially completed (2.2M records extracted, 209K loaded) but failed after 4h — one profile ID not authorized for Attribution streams. Need to fix in Airbyte config.
- **Amazon Seller Partner → BigQuery** connection also configured
  - Region: US (pulls US+CA+MX through one connection)
  - Start date: 2024-01-01
  - Selected streams: ListFinancialEventGroups, ListFinancialEvents (multiple event types), Orders, OrderItems, FBA inventory reports, returns, settlement reports, reimbursements
  - All columns selected per report (storage is cheap, backfilling is painful)
  - SA key (`openclaw-sheets@...`) used for BigQuery auth
- **GCP IAM**: SA got **BigQuery Data Editor** + **BigQuery Job User** roles added
- **Dataset**: `amazon_raw` in project `lustrous-bounty-460801-b9`, location `us`
- **Destination**: BigQuery using existing service account credentials

### Cron Jobs Affected
- **86ewr926h** (Daily Amazon Data Collection) → **DISABLE** once Airbyte confirmed working
- **86ewr926q** (Daily PPC Collection) → **DISABLE** once Airbyte confirmed working  
- **86ewr928d** (FBA Restock Alert) → **UPDATE** to query BigQuery instead of SQLite
- Other crons (Sales & Traffic, Strategy Report, Wholesale Pricing) — review after BigQuery confirmed
- **TODO**: Check BigQuery on Sat Mar 7 at 1 PM to verify data landed for both US and CA

### Data Architecture Decision
- **BigQuery** for all Airbyte-sourced data (orders, financial events, PPC, inventory)
- **SQLite** retained temporarily for: keyword rankings (DataDive), BSR (Keepa) — until migrated
- Long term: retire SQLite entirely, single data warehouse in BigQuery
- **No copying BQ→SQLite** — query BigQuery directly going forward

### Future Airbyte Sources (discussed, not yet connected)
- **Shopify** — wait until DTC volume picks up (after premium set launch)
- **QBO** — big priority for automated reconciliation/P&L
- **Klaviyo** — useful now for email metrics correlation with sales
- **Google Ads, Facebook Ads** — when ready for paid social
- **Google Search Console** — already have local collection

## Slack Image Support Fixed
- Slack bot token was missing `files:read` scope — couldn't see images shared in Slack
- Ramon added `files:read` (and `files:write`) to the Slack app OAuth scopes
- Reinstalled app to workspace, got new bot token
- Token left in `~/Downloads/slack.txt` → Chloe updated OpenClaw config and restarted gateway
- **Now confirmed working** — Chloe can see images shared in Slack channels

## Mac Mini Connectivity Issues
- NoMachine couldn't connect from Ramon's Windows PC
- SSH also not connecting (port 22 — sshd not enabled on Mac mini)
- Mac mini's local IP is `192.168.68.200` (was correct on Ramon's end)
- Root cause: network issue on Ramon's side (not the Mac mini)
- Ramon fixed it himself and got back into NoMachine
- Attempted reboot via `osascript` silently failed (needed elevated permissions)
- **Note**: SSH (Remote Login) is NOT enabled on the Mac mini — should be enabled for future troubleshooting

## Amazon Product Box — 3D Render Discussion
- Ramon considering getting a 3D model (OBJ) of his actual roll end tuck top box shape
- Current main image uses a flat rectangle render — actual box has tuck flap and different proportions
- Plan: Get OBJ made → apply artwork in Adobe Dimensions → render realistic product shots
- Could also create animated product video using After Effects + Cinema 4D Lite (included in Adobe CC)
- Element 3D plugin ($200) would be easier but not strictly necessary — try free tools first
- Fiverr recommendations given for OBJ modeling (Nickarts, Proidanis3d, and one more)
- Ramon has Illustrator dieline files for the box
- **Adobe Dimension** can import OBJ and apply materials, but can't model the box from a dieline
- **After Effects** with Cinema 4D Lite can do animation — no extra purchase needed to start

## ManyChat + Klaviyo Integration
- Ramon setting up ManyChat for Instagram/Facebook comment automation
- Trigger words: "START" and "SUNDAY" for Sunday Ritual email signup
- Flow: Comment → DM → Collect email in-app → Push to Klaviyo list
- **Create separate Klaviyo API keys for ManyChat** — don't share the ones Chloe uses
- Public key (site ID) is shared across integrations; private key should be unique per integration
- Limit ManyChat's private key scopes to: lists, profiles/subscriptions only

## Strategy Report Bug Fix
- `manage_strategy.py` had entries using `entry_id` instead of `id` (entries FB035, FB036)
- Patched script to accept both `entry_id` and `id` keys

## Canasta Group Scrape — 4 New Entries
- **CL020**: Opening with dirty closed canasta (5 aces + 2 wilds + clean triple) — allowed if meld points met
- **CL021**: Pack vs talon when melding — unanimous: ONE or the OTHER, never both
- **FB057**: Can't discard Joker even for Straight special hand; if stuck with dead cards after picking pack, take back last safe melded card
- **FB058**: Wild card as only card (details captured in strategy.jsonl)

## Daily Amazon Data Collection — Partial Failure
- Inventory collection failed for both US (FATAL) and CA (429 rate limit)
- Orders ran but returned 0 rows
- Logged to ClickUp as partial failure, alerted #chloe-logs

## Credential Access
- Ramon provided master password (verified) to export SP-API credentials for Airbyte setup
- Files placed in Downloads: `bigquery_service_account_key.json`, `airbyte-seller-central-creds.rtf`
- **Reminder**: Delete credential files from Downloads folder
