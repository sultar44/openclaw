# 2026-03-14

## Events
- Heartbeat at 06:06 AM EST.
- Performed cron health check and Daily Cron Audit (05:00-06:00 AM window slightly passed but handled).
- Performed Gmail OAuth health check (early for Monday, but verified working).

## Cron Audit Results
- **Runaway Check:** No jobs exceeded 10 runs in the last 2 hours.
- **Errors/Warnings:**
    - `Wholesale Pricing Report (12h)` (a64dc8b6): Last status `error` (timeout).
    - `Daily PPC Search Terms (6AM)` (da8f9e2e): Last status `error` (timeout).
    - `SP On-Demand: Sales & Traffic (9AM)` (98824211): 2 consecutive errors (`Message failed`).
    - `Daily Sales & Traffic Collection (10AM)` (93e6e259): 1 consecutive error (`Message failed`).
    - `daily-video-post` (d991a7a5): 1 consecutive error (`Message failed`).
- **Gmail OAuth:** Verified healthy via `gog gmail labels list`.

## Cron Timeout Fix (8:00-8:25 AM)
- **Root cause:** Last night's batch cron conversion (11:30 PM Mar 13) set `timeoutSeconds: 120` on all 18 cron_runner jobs
- **Impact:** Wholesale Pricing Report (86ewr926f) and Daily Sales & Traffic (86ewr926t) failed overnight due to timeout
- **Fix:** Recreated all 18 jobs with outer timeout = inner `--timeout` + 60s buffer
- **Also fixed:** Self-referencing `--cron-id` in payload messages (added aliases to clickup_config.json since recreating generates new IDs)
- **Lesson:** When batch-converting cron jobs, always match outer timeoutSeconds to the script's actual runtime needs

## GA4 Access Confirmed (12:15 AM)
- Ramon granted GA4 Viewer access to service account `openclaw-sheets@lustrous-bounty-460801-b9.iam.gserviceaccount.com`
- Verified working — pulled 7-day data: 228 users, 400 page views, ~2 min avg session
- **Decision:** Integrate GA4 data into the April 3 Monthly SEO Strategy Review (combine GSC + GA4 + DataDive)
- Ramon: "bake it into April 3" — no separate weekly GA4 reports for now
- GSC + GA4 is the complete Google data set; Klaviyo, Shopify, and Airbyte already connected

## SP Report Daily Status Cron Fix (12:27 AM)
- Root cause: The LLM session was trying to use `message` tool to post to Slack AND OpenClaw's `announce` delivery was also posting — duplicate, and the message tool was failing
- Fix: Updated prompt to tell LLM not to use message tool, set `bestEffort` delivery, switched to `gemini-flash` model
- Ramon asked: "do we need the LLM at all?" — No, but OpenClaw cron only has `systemEvent` and `agentTurn` payload types, no "just exec a shell command" mode. Flash is the minimum tax.

## Facebook Canasta Scraper Dud Run (Mar 13)
- Run lasted only 82 seconds (normal: 5-15 min), produced 846 tokens vs usual 10k-38k
- Session loaded context, wrote a plan ("Tonight's rotation: MAC and CL..."), then treated that as its final answer — no tool calls, no browser, no scraping
- Most likely cause: LLM behavior fluke — responded with plan as complete message instead of acting
- **Ramon's rule:** "1-in-15 dud rate is fine. Not mission critical. Investigate only on back-to-back duds."

## Amazon Ads Reauth Process (8:52-9:08 AM)
- Amazon Ads session expired, required manual login in OpenClaw browser
- Successfully logged in without 2FA this time
- Disabled Chrome passkey popup (modified Preferences to suppress WebAuthn conditional UI)
- Created `playbooks/amazon-ads-reauth.md` with step-by-step process
- Added note to TOOLS.md about stored credentials for operational use
- **Ramon's preference:** No proactive session monitoring needed. "Do it as you need it. Let's see what happens tomorrow."
- **Key insight:** Master password is for *revealing* credentials to someone asking — not for Chloe using them operationally. Chloe can access `.env` directly for routine tasks.

## Video Content Swap (10:27 AM - 11:24 AM)
- V18 (The Sunday Dinner) — file missing from Drive, agency sent for editing fixes
- V02 (Estée Lauder) — also missing, same reason
- **Solution:** Swapped V14 (Malala, Apr 4 slot) into today's slot. V18 pushed to Apr 4.
- Ramon: "Grab the last video, slot it in. Don't rename everything."
- Agency will deliver V18 and V2 by Wednesday (before V02's scheduled date of Mar 27)
- Video posted: V14 Malala on all 4 platforms, marked posted, file moved to Posted folder

## Social Media Best Practices Discussion (11:28 AM)
- **Pinned comments:** Stop pinning "Comment START" CTA. Use pinned comment for conversation starters instead. Keep CTA in description only. ManyChat triggers on any comment with keyword regardless.
- **Thumbnails:** Ask agency to include a 1-second title card frame at start of each video (Batch 2). All platforms allow frame selection for cover. 1 second > 1 frame (easier to find when scrubbing).
- **Hashtags:** 3-5 specific hashtags is optimal. Avoid generic (#love, #inspo). Include 1 branded (#all7sgames).
- Ramon will implement these changes for Batch 2 videos.

## Airbyte Decision
- Ramon: "Airbyte fails don't bother me anymore. At this point I feel confident we don't need them anymore. We said until Monday."
- Plan: Monday (Mar 16) — evaluate shutting down Airbyte entirely

## eBay Sync Bugs Fixed (8:42-8:50 AM)
- **Bug 1: Duplicate listings** — Script stripped SKU suffixes but didn't check if ASIN already listed under a different SKU. Created duplicates for Alegria shoes. Fix: ASIN-based dedup check before creating new listings.
- **Bug 2: Hardcoded $16.99 price** — was applying to all products including $37-40 shoes.
- **Fix:** Price reads from Ebay tab column E. New listings default $16.99. Manual override via sheet.
- **Ramon's rule:** "All clothing should be $16.99 on eBay. Shoes at $29.99."
- **Added:** Skip no-op API calls — compare current qty/price before calling eBay API
- Ended 2 duplicate listings (147203825639, 147203825782)

## SP Report Status Checker Fixes (8:05-8:10 PM)
- **Bug 1: UTC timezone** — Status checker ran at 8 PM EST = midnight UTC, looked for downloads on the new UTC date (Mar 15) instead of looking at what actually downloaded today. Fix: 24-hour lookback window instead of UTC midnight.
- **Bug 2: Multi-marketplace reports** — Reports with marketplace "US, CA" weren't matching individual "US" or "CA" lookups. Fix: split multi-marketplace strings when building downloaded_keys.
- Result: 15/16 reports actually downloaded fine; only 3 CA stragglers pending (normal Amazon timing)

## Ads Email Reports Pipeline — WIRED UP (6:08-6:16 PM)
- **Critical context:** Ramon's plan is to eliminate both Airbyte AND direct API pulls by Monday, keeping ONLY the 5 daily Amazon Ads email reports as primary data source
- `ads_report_processor.py` existed and worked but was never called by the Gmail router — just getting archived
- **Fixed:** Wired `handle_ads_report` in Gmail router to call the processor (extract attachment → parse → load to BQ → archive)
- Fixed report type misdetection: "Targeting" and "Placement" reports were being detected incorrectly
- 5 report types: Campaign, Placement, Targeting, Advertised Product, Search Term
- Both US and CA data in each report
- BQ tables: `ads_console_campaigns`, `ads_console_search_terms`, `ads_console_targets`, `ads_console_placements`, `ads_console_products`

## Gmail Inbox Processor Cron Fix (6:41 PM)
- 3 consecutive "Message failed" errors — job was trying to announce output but failing
- Ramon: "This cron shouldn't send any alert at all. Either has email and processes (we get the email alert) or no email and no alert."
- Fix: Set `bestEffort` delivery, disabled failure alerts for this job
- Runs silently when inbox empty, per-email alerts to #chloelogs when processing

## PO Forecast Alert Fixes (8:30 PM)
Two fixes to `po_forecast_alert.py`:
1. **Active PO filter bug** — blank status in Purchase_Orders was being treated as "no PO" instead of "in progress." Fixed: any row where status != "Delivered" = active PO.
2. **Seasonal SKU handling** — Added `seasonal_target_arrival` and `seasonal_qty` columns to Product_List sheet. HWC set to target arrival 9/15/2026, qty 2000. Seasonal SKUs are excluded from regular restock alerts entirely, get their own "Seasonal PO Reminders" section.
- HWC lead time currently 90d (may drop to 60d after Ramon contacts supplier ~May)
- Seasonal alert will fire 45 days before PO due date (~May 3 at 90d, ~Jun 1 at 60d)
- If PO is placed (row in Purchase_Orders with blank status), alert shows "PO already placed"
- Ramon: "now that we have more control I don't need extra buffer time"

## FBA Reimbursement Audit System — MAJOR BUILD (8:43 PM - 9:29 PM)

Ramon's priority list for next projects:
1. ~~PO Projection~~ ✅ DONE
2. **FBA Reimbursement Generator** ← Built tonight
3. Review collection + bad review alerts (next)
4. Competitor monitoring
5. PPC

### What was built
Four new Python scripts in `~/amazon-data/collectors/`:
- `reimbursement_reports.py` — pulls 4 SP-API reports (Inventory Ledger, Customer Returns, FBA Fees, Replacements) → BQ tables (`sp_inventory_ledger`, `sp_customer_returns`, `sp_fba_fees`, `sp_replacements`)
- `reimbursement_audit.py` — 6 audit types, pure Python/SQL, zero LLM:
  1. Inbound Shipment Shortages (9-month window)
  2. Lost in Warehouse (60-day window)
  3. Damaged in Warehouse (60-day window)
  4. Customer Returns Not Received (60-120 day window)
  5. Returns Received but Not Restocked (90 day window)
  6. FBA Fee Overcharges
- `reimbursement_alert.py` — weekly formatted alert with urgency tiers (🔴/🟡/🟢)
- `reimbursement_reconciler.py` — cross-references BQ reimbursements data against Cases sheet to auto-detect approved/denied/auto-reimbursed cases
- `reimbursement_case_tracker.py` — processes forwarded case emails (creation + responses), matches to Cases tab, auto-updates status

### Sheet structure
- Product Master sheet (`1MUcmj4mqz-N5QSRFpqrbFUOTX33EKo85CDoaAXuuSGs`)
- **`FBA_Reimbursements` tab** — 201 raw detail rows, each with `case_text` (copy/paste for Seller Central) and `case_group` linking to Cases tab
- **`Cases` tab** — 16 cases (after date clustering split from 11). Columns include: case_group, type, sku, asin, total_units, est_value, case_text, status, case_id, case_filed_date, resolution, resolution_amount, transaction_ids, case_url (auto-generated)

### Case date clustering
- Ramon: "Split them. Let's not give these people the chance to disregard cases."
- Cases split by 3-day window clusters (transactions >3 days apart = separate cases)
- 11 original groups → 16 cases after splitting
- Example: CAN3P Lost had 3 date clusters (Jan 19, Jan 28, Mar 2) → 3 separate cases

### Case tracking via email
- Ramon set up Gmail filter: `from:(amazon.com) "Thank you for creating a new Case:"` → forward to chloemercer32@gmail.com
- Case response filter TBD — `from:(amazon.com) "regarding Case"` didn't yield results; will build from real data after first case is filed
- Case tracker handles 5 scenarios: Created, Action Needed (→ #chloebot alert), Approved, Denied, Closed
- Case URL template: `https://sellercentral.amazon.com/cu/case-dashboard/view-case?ref=sc_cd_lobby_vc_v3&ie=UTF&caseID=XXXXX&View+or+Respond=Submit`
- TRID (Transaction Reference ID) needed for Seller Central case form — added as separate field in Cases tab

### Cron jobs
- `794f8248` — Weekly Reimbursement Reports (Sun 11PM) — ClickUp `86ewy25ca`
- `7a36e0cd` — Weekly Reimbursement Audit (Mon 10AM) — ClickUp `86ewy25cb`
  - Runs reconciler → audit → alert, announces to #ops_amazon (CF9T43YMQ)

### Key findings from first run
- 128 active opportunities after filtering inactive SKUs (was 201 before filter)
- NER-STD (73 rows, 150 units, ~$2,250) = **FALSE POSITIVE** — those were disposal orders Ramon requested due to CPC compliance issue. Disposal orders: `2602251AZ8` (35 units), `260301UV7` (115 units). Marked as False Positive.
- Added disposal cross-reference to audit logic: SKUs with completed disposal orders in `sp_removals` are excluded from "Lost in Warehouse"
- Actual recovery estimate: ~$1,058 across 16 cases (after date clustering)
- Amazon had already auto-reimbursed one case (GTV damaged warehouse, $8.05) without Ramon filing

### Architecture decisions
- **No email forwarding needed for resolution tracking** — Amazon's Reimbursements Report in BQ has `case_id` column. Reconciler checks BQ weekly.
- Email forwarding IS used for case creation tracking and action-needed alerts
- Ramon's workflow: copy case_text → paste in Seller Central → type Case ID in sheet → reconciler auto-tracks resolution
- Amazon reason codes in ledger: Q = transferred to defective (often before disposal), M = misplaced, P = transfer to defective disposition, F = found
- Self-learning system saves approval rates to `collectors/reimbursement_learnings.json`
- Ramon: "I always lean towards less work if possible"

### Policy changes noted (from research)
- As of Mar 2025: reimbursements for inventory lost BEFORE customer order = based on manufacturing cost, not sales price
- Claim windows: Lost/damaged warehouse = 60 days, Customer returns not received = 60-120 days, Inbound shipments = 9 months, Fee overcharges = 90 days

## Ramon Filed First Case (9:59 PM)
- Started filing cases in Seller Central, hit TRID field in the damaged items form
- TRID = transaction reference ID from inventory ledger (e.g., `20075449481030`)
- Added transaction_ids as a separate column in Cases tab for easy copy/paste

## Airbyte Health Check (8:00 AM)
- SP connection: 3 consecutive failures (Mar 12-14), low row counts (~800-1000 vs normal)
- Ads connection: failed but auto-retried successfully
- Ramon not concerned about Airbyte failures anymore (per above decision)

## Facebook Canasta Scraper Results (10:30 PM)
3 new entries tonight:
- **FB073** (MAC) — Special hand 3-3-3-3-2: must pair be wild cards? Majority yes in MAC rules, table rule allows any pair.
- **CCG002** (CCG) — Can wild cards go on other melds after starting wild card canasta? Split: MAC says no (locked until closed), classic allows it.
- **CL024** (CL) — Does going-out discard follow empty tray restrictions? Near-unanimous: No — going out = face down = any card.

## Claude Code CLI
- Claude Code (`claude` v2.1.71) is installed but NOT logged in on Mac mini. Can't use for spawning coding agents.
- Workaround: built everything directly in the session
