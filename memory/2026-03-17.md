# 2026-03-17 - Daily Memory

## Cron Infrastructure Overhaul

### SELF Resolution for Cron Jobs (9:34 AM)
- **Problem:** When cron jobs get recreated, their payload had a hardcoded `--cron-id <UUID>` that went stale, breaking ClickUp logging
- **Fix:** Added SELF resolution to `cron_runner.sh` — when `--cron-id SELF`, it queries `openclaw cron list --json` and matches the job by the `--command` string in the payload
- **Converted all 41 cron jobs** from hardcoded UUIDs to `--cron-id SELF`
- **Result:** Cron jobs can now be recreated freely without breaking ClickUp integration. The ID mismatch bug is permanently eliminated.
- Also gracefully handles unresolved SELF (skips ClickUp logging with warning instead of crashing)

### Multiple ClickUp Mapping Fixes
- Restored `210cc71d` (Financial Events) from archived section back to `cron_to_task`
- Added `a68e1ec7` (AWD Inventory) → `86ewr926x` mapping
- Added `b34f8dbf` (Inventory Planning inner ID) → `86ewwxapq` mapping
- Added `93578f5c` (Merchant Listings inner ID) → `86ewwxapt` mapping
- Added `a64dc8b6` (Wholesale Pricing inner ID) → `86ewr926f` mapping
- Fixed both rank cron jobs that had stale `--cron-id` values in payloads

## Data Collection Cleanup (10:00 AM)
Audited all data collection pipelines with Ramon. Created `playbooks/data-collection-map.md` as source of truth.

### Disabled 3 redundant crons:
1. **Daily PPC Search Terms** (`6891e1e4`, ClickUp `86ewr926q`) — replaced by email pipeline (ads_report_processor.py)
2. **Daily Sales & Traffic 10AM** (`44b09b0f`, ClickUp `86ewr926t`) — duplicate of on-demand S&T 9AM (70 cols vs 16)
3. **SQP Retry on-demand** (`8c9c9c11`, ClickUp `86ewr9282`) — scheduled+poller handles SQP with richer data

### Also:
- Removed S&T from scheduled_report_poller.py watch list (on-demand 9AM handles with 3-day lookback)
- Kept SQP in poller (now the primary SQP collection path)
- Frozen archive tables: ppc_search_terms, sales_daily, sales_daily_totals, search_query_performance
- Added "Data Collection Map" tab to Cron Registry sheet for Ramon's visibility
- SQP polling optimized: only polls Mon-Wed, stops once collected for the week

### Key insight from Ramon:
- PPC data should come ONLY via email (Amazon Ads Console scheduled reports)
- SP-API: prefer scheduled reports + hourly poller over create-on-demand
- On-demand only for reports that can't be scheduled (S&T with lookback, Inventory Planning, Merchant Listings)

## Amazon Ads Report Sender Change
- Amazon changed the Ads report sender from `no-reply@amazon.com` to `no-reply@ads.amazon.com`
- This broke the auto-forward rule; Ramon set up a new forward for the new address
- Gmail inbox router updated to match both addresses
- BQ data confirmed complete through Mar 16 for all 5 types except Search Terms (arrives daily at ~2 PM)
- Old unprocessed report emails marked as read to prevent reprocessing

## Review Scraper Improvements

### Parent-Family Dedup
- Fixed review_scraper.py to dedup across entire parent family (B07KBB1WJS + B0BR4FDDV8 + B0G1NRM21J)
- Two layers: same review_id across any ASIN in family → skip; same author+title across family → skip
- Cleaned existing data: deleted 20 duplicate child rows from BQ (297 → 277 rows)
- Dropped 2 child ASINs from scrape list (8 ASINs instead of 10) — same coverage, less browser time

### Variation Field Added
- Each review now captures `variation` from `[data-hook="format-strip"]` (e.g., "Edition: Classic Edition")
- New `variation` column added to BQ table
- Shows up in negative review alerts so Ramon knows which variant got hit
- Existing 731+ rows have blank variation — will be backfilled when Ramon manually rescrapes

### Chrome Extension Updated (v1.1.0)
- content.js: extracts variation field
- review_server.py: parent-family dedup + variation field + backfill logic
- popup.js: shows variation in preview + parent dupe reporting + backfill count
- Backfill path: if review exists but variation is empty, UPDATE instead of skip
- Files pushed to Google Drive folder
- Ramon plans to rescan on Thursday to backfill variation data

### Review Scraper Status Logic Fix
- Changed: finding negative reviews no longer marks as `partial_failure` — that's the script doing its job
- Now: `partial_failure` only when ASINs actually fail to scrape (0 reviews + no data)

## Gmail Circuit Breaker Overhaul
- **Problem:** Circuit breaker was set to 5 emails in 60s with permanent lock — way too tight
- **Root cause of repeated trips:** Ramon forwarded 4 Ads report emails + HARO emails in quick succession
- **Also discovered:** Was resetting wrong file path (`transforms/` vs `hooks/`)
- **Fix applied:**
  - Raised threshold: 20 emails per 5-minute window (handles real bursts)
  - Auto-resets after 5 minutes (no more permanent lockout)
  - Alerts once when tripped, then silently drops until cooldown (no more message spam)

## Gmail Watcher Correction
- **Important:** The gog watcher IS the primary email processing system, NOT broken
- Gmail Inbox Processor (polling cron) is the BACKUP safety net
- Corrected my own memory — had wrongly noted the watcher was broken/replaced
- Watcher is alive (PID 17206), subscription valid through Mar 24

## PR Outreach

### Follow-ups Drafted & Sent (7 Email 2 follow-ups)
All from the 3/3 batch hitting the 14-day mark:
1. The Guardian (Owen Duffy)
2. IGN Southeast Asia (Sam Desatoff)
3. In Our Spare Time (Ellen LaFleche-Christian)
4. Sixty and Me (Margaret Manning)
5. AARP (Lois Joy Johnson)
6. Seniors Lifestyle Magazine (Gail Willowby)
7. Retirement Genius (Chris Orestis)

### BCC Learning Loop Fix
- `pr_bcc_processor.py` advance map was missing `Email 2 Ready → Sent 2` and `Email 3 Ready → Sent 3` transitions
- Fixed — now covers all status transitions properly
- Post-3-emails flow: "No Response" → dormant until article refreshes (~yearly) → fresh 3-email cycle. Two full cycles = "Exhausted"

### PR Followup Check Timeout
- Was timing out at 180s when drafting 7 emails
- Ramon approved bumping timeout to 360s

### Ginger Casa Opportunity Rejected
- HARO evening edition found Ginger Casa (Mother's Day Gift Guide, score 95/100)
- Ramon replied: already tried ~2 weeks ago, she declined. Closed the opportunity.

### AARP Pitch Rework
- Ramon requested rework — sounded like talking about himself in 3rd person
- Rewrote in first person with real example (Wood & Fire Canasta meetup in Delray Beach)
- AI humanizer score: 12/100 (good)

## Influencer Discovery (Weekly, Tue 1AM)
- **7 influencers added** (3 priority targets):
  - ⭐ @mahjongmolly (84K), ⭐ @stylinbrunette (80K), ⭐ @larasmahjongedit (68K)
  - @themahjongsocialclub (8.3K), @mahj.mama (1.5K), @holycitymahjong (1.3K), @kansascitymahjong (1K)
- **15 brands added** (3 priority watch):
  - 🔴 @ohmymahjong (205K IG, 28.6K TikTok)

## Daily Video Post
- Martha Stewart (Famous People series) — S01_V10_FP
- Cron timed out at 180s due to resource contention (Ramon was hammering the Mac mini)
- Manual rerun succeeded; sheet marked posted, video moved to Posted folder
- Ramon confirmed: don't bump timeout, it was a one-time resource issue

## Intertek CPC Testing Status
- **SH2 (Scavenger Hunt):** ✅ APPROVED — CPC cleared
- **TH (Treasure Hunt):** ⚠️ PENDING — needs measurable label artwork file + physical jewels for sharp point retest
- **RJ (Rolling Jokers):** ⚠️ PENDING — still need acrylic (compliant) marbles for lead/chemical retest
- **RJ3P (Rolling Jokers 3-Pack):** ⚠️ PENDING — label issue like SH2, but Cathy wants measurable artwork file (not just a photo)
- Ramon sent her a label image; needs to send the actual PDF/Illustrator file

## Inventory & Restock
- AWD stock: A7S-CANSET 1,800 units (75 ctns), GTV-SH2 432 (18 ctns), A7S-CAN2P 200 (5 ctns), A7S-CAN6P 96 (8 ctns), NER-RJ 24 (3 ctns)
- 5 SKUs at 0 units (HWC + 4 Trivia SKUs)
- CAN2P US inventory question from Ramon: with 5 cartons left in AWD and nothing else, by when do we run out?

## Listing Monitor Alert
- A7S-TTFRA (US) strikethrough disappeared — was showing -25% discount

## Google/YouTube Linking
- Ramon got email about auto-linking Google services to an old "Board Game Fun" YouTube channel
- Recommended: opt out, don't link dead assets, keep personal channel separate
- Ramon says he has no opt-out option — only wait or link
- Advised checking Merchant Center → Settings → Linked accounts → YouTube

## Shopify Navigation Discussion
- Discussed adding Contact Us and Replacement Parts to top nav on all7s.co
- Recommended "Support" dropdown (Option A) over standalone links to avoid crowding

## Cron Health Issues Noted
- `daily-video-post`: Had 4-5 consecutive errors (timeouts from resource contention)
- `Listing Monitor (Tue-Fri Changes)`: 3 consecutive errors (delivery announcement failing, not actual job failure)
- Both Listing Monitor and IDR: job works fine, just delivery `--no-deliver` flag discussion (Ramon said don't change anything)
