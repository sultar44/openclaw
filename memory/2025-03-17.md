
## BCC Learning Loop Fix (5:30 PM)
- Ramon caught that forwarded HARO pitch response (AARP / Nicole Pajer) was archived but NOT processed through `pr_bcc_processor.py` to advance the sheet row
- Root cause: `gmail-security.js` trusted sender prompt was too vague — said "follow that playbook" without explicit instructions on HOW to detect and process BCC'd/forwarded pitches
- Fix: Added explicit BCC Learning Loop detection instructions to the trusted sender message in `gmail-security.js`:
  - Detect forwarded/BCC'd pitches from ramon@goven.com or ramon@all7s.co
  - Extract reporter email from forwarded content
  - Run `pr_bcc_processor.py --action advance` to update sheet status
  - Study Ramon's edits for style learning
  - Handle decline emails with `--action close`
- The `gmail_inbox_router.py` (polling backup) already had this logic in `handle_bcc_learning()` — only the real-time watcher was missing it
- Manually fixed the AARP row: Draft 1 Ready → Sent 1
